FLIGHT SIMULATOR USING CESIUM LIBRARY


IDE: Visual Studio Code (VS Code) - extensions: ESLint, Prettier

Init project:

npm create vite@latest symulator-cesium -- --template vanilla-ts

cd symulator-cesium

npm install cesium

npm install vite-plugin-cesium
